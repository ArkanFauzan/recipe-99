import React from "react";

// is a context for global state variabel
// BASE_URL
// user: status (login or not), id, name, email
// function cookie (setCookie and getCookie)
const GlobalContext = React.createContext();
export { GlobalContext };
